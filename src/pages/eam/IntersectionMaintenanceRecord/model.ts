export interface ModalState {
}

export interface ModelType {
  namespace: string;
  state: ModalState;
  reducers: {
  };
  effects: {
  };
}

const initState = {
};

const Model: ModelType = {
  namespace: 'IntersectionMaintenanceRecord',
  state: initState,
  effects: {
  },
  reducers: {
  },
};

export default Model;
