export interface ModalState {
}

export interface ModelType {
  namespace: string;
  state: ModalState;
  reducers: {
  };
  effects: {
  };
}

const initState = {
};

const Model: ModelType = {
  namespace: 'roadTrafficSignal',
  state: initState,
  effects: {
  },
  reducers: {
  },
};

export default Model;
